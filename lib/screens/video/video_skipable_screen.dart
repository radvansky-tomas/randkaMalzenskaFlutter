import 'package:flutter/cupertino.dart';

class VideoSkipableScreen extends StatelessWidget {
  final bool showControll;
  final String videoUrl;

  VideoSkipableScreen(this.showControll, this.videoUrl);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
